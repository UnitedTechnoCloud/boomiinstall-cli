{
  "atomId" : "${atomId}",
  "url" : "${url}",
  "minAuth" : "${auth}",
  "apiType" : "${apiType}",
  "overrideUrl" : "${overrideUrl}",
  "minAuth" : "basic",
  "auth" : "basic", 
  "externalHttpsPort": "443"	
}
